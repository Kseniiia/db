CREATE TABLE ROLE (
	ID NUMBER GENERATED BY DEFAULT AS IDENTITY,
	NAME VARCHAR2(255) NOT NULL,
	CONSTRAINT ROLE_PK PRIMARY KEY (ID)
);

COMMENT ON COLUMN ROLE.ID IS 'Unique identifier of the table ROLE';
COMMENT ON COLUMN ROLE.NAME IS 'Name of role';